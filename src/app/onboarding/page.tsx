import{redirect}from"next/navigation";import{auth}from"@/auth";import{db}from"@/lib/db";async function save(form:FormData){"use server";const session=await auth();if(!session?.user.id)redirect("/login");const capital=String(form.get("capital")||"0");await db.$transaction([db.userProfile.update({where:{userId:session.user.id},data:{country:String(form.get("country")),baseCurrency:String(form.get("currency")),primaryMarket:String(form.get("market")),experienceLevel:String(form.get("experience")),tradingStyle:String(form.get("style")),completedAt:new Date()}}),db.capitalTransaction.create({data:{userId:session.user.id,effectiveAt:new Date(),kind:"STARTING",amount:capital}}),db.auditLog.create({data:{userId:session.user.id,action:"ONBOARDING_COMPLETED",entityType:"UserProfile"}})]);redirect("/dashboard")};export default function Onboarding(){return<main className="auth-shell"><section className="auth-card wide"><p className="eyebrow">SET YOUR BASELINE</p><h1>Configure risk context</h1><p className="muted">Capital is dated so deposits and withdrawals never hide trading performance.</p><form action={save} className="form-grid"><label>Country<select name="country"><option value="IN">India</option><option value="US">United States</option><option value="GB">United Kingdom</option></select></label><label>Base currency<select name="currency"><option>INR</option><option>USD</option><option>GBP</option></select></label><label>Starting capital<input name="capital" type="number" min="1" step="0.01" required/></label><label>Primary market<select name="market"><option>Indian equities & derivatives</option><option>Commodities</option><option>Currency derivatives</option></select></label><label>Experience<select name="experience"><option>Beginner</option><option>Intermediate</option><option>Advanced</option><option>Professional</option></select></label><label>Trading style<select name="style"><option>Intraday</option><option>Swing</option><option>Positional</option><option>Investing</option><option>Scalping</option></select></label><button>Open analytics workspace</button></form></section></main>}

