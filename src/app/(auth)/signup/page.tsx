import Link from"next/link";import{register}from"@/app/actions";export default async function Signup({searchParams}:{searchParams:Promise<{error?:string}>}){const{error}=await searchParams;return<section className="auth-card"><p className="eyebrow">PRIVATE BY DEFAULT</p><h1>Create your workspace</h1><p className="muted">Start with one secure trading account. Add brokers later.</p>{error&&<div className="error">{error}</div>}<form action={register}><label>Name<input name="name" autoComplete="name" required minLength={2}/></label><label>Email<input name="email" type="email" autoComplete="email" required/></label><label>Password<input name="password" type="password" autoComplete="new-password" minLength={12} required/><small>At least 12 characters</small></label><button>Create secure account</button></form><p className="switch">Already have an account? <Link href="/login">Sign in</Link></p></section>}

