import Link from"next/link";import{login}from"@/app/actions";export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const{error}=await searchParams;return<section className="auth-card"><p className="eyebrow">WELCOME BACK</p><h1>Sign in to your workspace</h1><p className="muted">Continue your private performance review.</p>{error&&<div className="error">{error}</div>}<form action={login}><label>Email<input name="email" type="email" autoComplete="email" required/></label><label>Password<input name="password" type="password" autoComplete="current-password" minLength={12} required/></label><button>Sign in</button></form><p className="switch">New to TradeRisk? <Link href="/signup">Create an account</Link></p></section>}

