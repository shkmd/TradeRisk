import{describe,it,expect}from"vitest";import*as XLSX from"xlsx";import{parseWorkbook}from"./importer";describe("broker-neutral importer",()=>{it("detects headers, skips totals, and removes duplicates",()=>{const rows=[["Broker report"],["Trade Date","Symbol","Qty","Gross P&L","Charges","Net P&L"],["2025-01-01","NIFTY",25,110,10,100],["2025-01-01","NIFTY",25,110,10,100],["Total","",25,110,10,100]],wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.aoa_to_sheet(rows),"F&O");const result=parseWorkbook(XLSX.write(wb,{type:"array",bookType:"xlsx"}));expect(result.trades).toHaveLength(1);expect(result.duplicates).toBe(1);expect(result.sheets).toEqual(["F&O"])});it("activates limited mode when dates are unavailable",()=>{const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.aoa_to_sheet([["Trade Date","Symbol","Qty","P&L"],["","INFY",1,10]]),"Trades");expect(parseWorkbook(XLSX.write(wb,{type:"array",bookType:"xlsx"})).limited).toBe(true)})});

